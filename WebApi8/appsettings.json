{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Configurando a conexão do banco
  // estamos utilizando apenas 1 (DefaultConnection).
  // Porém poderiamos ter várias, com seus proprios nomes
  "ConnectionStrings": {
    "DefaultConnection": "server = LATITUDE3540-CO; database = LibraryApi; trusted_connection = true; trustservercertificate = true"
    // trusted_connection: diz que eu sou um usuário do windows confiavel sem ter que settar 'user login = meuLogin; password = minhaSenha;'
    // trustservercertificate: aplica meu certificado de autenticação, do SQL SERVER
  },
  "AllowedHosts": "*"
}
